package codejam.OneA;

public class Pylons {
    /*
    static int[] cordX = {1,1,2,2,-1,-1,-2,-2};
    static int[] cordY = {2,-2,1,-1,2,-2,1,-1};

    boolean limits(int x, int y) {
        return ((x >= 0 && y >= 0) && (x < N && y < N));
    }

    boolean isempty(int a[], int x, int y) {
        return (limits(x, y)) && (a[y*N+x] < 0);
    }

    int getDegree(int a[], int x, int y) {
        int count = 0;
        for (int i = 0; i < N; ++i)
            if (isempty(a, (x + cordX[i]), (y + cordY[i])))
                count++;

        return count;
    }

    public static void main(String[] args) {

    }
    */
}
